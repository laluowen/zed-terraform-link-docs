[package]
name = "terraform-link-docs-lsp"
version = "0.0.1"
edition = "2021"
publish = false

[[bin]]
name = "terraform-link-docs-lsp"
path = "src/main.rs"

[dependencies]
lsp-server = "0.10"
lsp-types = "0.97"
serde_json = "1"
serde = { version = "1", features = ["derive"] }
regex = "1"
